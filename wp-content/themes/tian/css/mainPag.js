$(function() {
	$("#postTitles").scrollable({clickable:false}).circular(); 
	$("#twitTitles").scrollable({clickable:false}).circular();
	$("#flickPotos").scrollable({clickable:false}).circular();
	$("#lastSongos").scrollable({clickable:false}).circular();
});